
'use client';

import { useIntercom } from './IntercomMessenger.tools.js';


/* -----------------------------------------
  Install Intercom
----------------------------------------- */
const IntercomMessenger = () => {
  // just wrap useIntercom
  useIntercom("ugrxoiig");
  // no render
  return null;
};

export default IntercomMessenger;
